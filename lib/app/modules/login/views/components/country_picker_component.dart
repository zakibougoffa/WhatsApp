import 'package:flutter/material.dart';

class CountryPickerComponent extends StatelessWidget {
  const CountryPickerComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
